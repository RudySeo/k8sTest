apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "board-service.fullname" . }}-test-connection"
  labels:
    {{- include "board-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "board-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
